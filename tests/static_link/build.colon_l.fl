# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Aymeric Wibo

import bob

let foo = Linker([]).archive(Cc([]).compile(["lib1.c"]))
let cmd = Linker(["-l:libfoo.a"]).link(Cc([]).compile(["main.c"]))

install = {
	foo: "lib/libfoo.a",
	cmd: "bin/cmd"
}
